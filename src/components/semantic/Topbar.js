import React from 'react';
import GoogleBtn from '../function/LoginBtn';

const Topbar = () => {
    return (
        <div>
            <GoogleBtn /> 
        </div>
    );
};

export default Topbar;